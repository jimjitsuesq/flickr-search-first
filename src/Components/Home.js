import React from "react";

const Home = () => (
  <div>
    <h3>Welcome to my Flickr search!</h3>
    <p>
      Search for anything you like or select a pre-determined search using the
      buttons above.
    </p>
  </div>
);

export default Home;
